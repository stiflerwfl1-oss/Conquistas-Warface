# Process Manager for Heroku / Railway / Render
# Procfile define o tipo de processo e comando

web: node bot.js

# Para Heroku, o processo 'web' é obrigatório (porta $PORT)
# O bot usa healthcheck na porta 3000, que é mapeada internamente
